name: whatskart
description: A WhatsApp based shoping App

version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.5.0
  cloud_firestore: ^2.5.0

  url_launcher: ^6.0.9
  badges: ^2.0.1
  provider: ^5.0.0
  url_strategy: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true